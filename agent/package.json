{
  "name": "arbeiten-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Agent interface (CLI + MCP server) for the Arbeiten todo app.",
  "bin": {
    "arbeiten-agent": "dist/cli.js"
  },
  "scripts": {
    "build": "node build.mjs",
    "mcp": "node dist/mcp.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "better-sqlite3": "^11.5.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^22.9.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}
